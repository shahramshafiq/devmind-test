# devmind-test

### Paginate Function
The `paginate()` function paginates a list into chunks of a specified size. It correctly handles the last page when the list length is exactly divisible by `page_size`.

### Usage
To use the `paginate()` function, simply call it with a list and a page size: